# Local development only. `npm run dev:backend` writes these to `.env.local`.
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=

# On Vercel, set only NEXT_PUBLIC_CONVEX_URL to the URL of the same Convex
# development deployment. Do not set CONVEX_DEPLOYMENT or CONVEX_DEPLOY_KEY.
